<img
    :class="['ResponsiveImage', class] | cls"
    :src="default"
    :width="width"
    :height="height"
    :loading="loading | default('lazy')"
    :srcset="srcset"
    :alt="alt | default(image.alt)"
    @attributes
/>