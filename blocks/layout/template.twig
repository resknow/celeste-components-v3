<BlockWrapper :attrs="attributes" :block="block" :preview="is_preview">
    <SkipLink @if="not is_preview" />
    <App>
        <InnerBlocks />
    </App>
    <BackToTop @if="not is_preview" />
</BlockWrapper>