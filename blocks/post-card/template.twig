<BlockWrapper :attrs="attributes" :block="block" :preview="is_preview">
    <PostCard
        @if="fields.post_id or fields.query_type == 'auto'"
        :id="fields.query_type == 'manual' ? fields.post_id : post_id"
    />
    <PostCard.Placeholder
        @unless="fields"
    />
</BlockWrapper>